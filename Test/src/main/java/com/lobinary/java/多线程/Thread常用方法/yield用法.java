package com.lobinary.java.多线程.Thread常用方法;

/**
 * yield的真正用途是：使当前线程从执行态变为可执行态，也就是就绪态吧。
 * cpu会从众多的可执行态里选择，也就是说，当前也就是刚刚的那个线程还是有可能会被再次执行到的，
 * 并不是说一定会执行其他线程而该线程在下一次不会执行到了。
 */
public class yield用法 {
	
	public static void main(String[] args) {
		//暂时没发现具体用处
	}

}
